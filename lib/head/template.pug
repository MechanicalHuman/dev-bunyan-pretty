span
    #{self.color} #{self.loggerId} ❯
if self.message
    item #{self.message}
if self.tag
    item
        dimmed #{self.tag}
if self.time
    item
        dimmed #{self.time}
if self.src
    src #{self.src}
